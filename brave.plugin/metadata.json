{
	"icon": "brave",
	"label": "Brave Browser",
	"description": "A privacy focused browser that blocks ads and trackers by default. May not work with current Kernel",
	"license": "Mozilla Public License 2.0",
	"libre": true,
	"category": "Apps",
	"scripts": {
		"exec": {
			"label": "Install",
			"command": "run-as-root -s install.sh"
		},
		"undo": {
			"label": "Remove",
			"command": "run-as-root apt -y remove brave"
		},
		"status": { "command": "dpkg-query -l brave" }
	}
}
